namespace BSU.BRSM.Bot.Core.Tests;
public class UserServiceTests
{
    [Fact]
    public void GetByQuery_WithChatId_CallsGetByChatId()
    {
        //var userRepositoryStub = new Mock<IUserRepository>();
        //userRepositoryStub.Setup(x => x.GetByChatId(It.IsAny<long>())).
        //    Returns(new[] { new User(1, "", "") });

        //userRepositoryStub.Setup(x => x.GetByFirstOrLastName(It.IsAny<string>())).
        //    Returns(new[] { new User(2, "", "") });

        //var userService = new UserService(userRepositoryStub.Object);

        //var validChatId = "93485546";

        //var actual = userService.GetByQuery(validChatId);

        //Assert.Collection(actual, user => Assert.Equal(1, user.ChatId));
    }
}